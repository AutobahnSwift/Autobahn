os:
    - linux
    - osx
language: generic
sudo: required
dist: xenial
osx_image: xcode9.0
before_install:
  - '[ $TRAVIS_OS_NAME == "linux" ] && sudo apt-get install swift-beta'
script:
    - swift build
    - swift build -c release
    - swift test
