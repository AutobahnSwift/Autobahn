os:
    - linux
    - osx
language: generic
sudo: required
dist: xenial
osx_image: xcode9.0
before_install:
  - if [ $TRAVIS_OS_NAME == "linux" ]; then
        sudo apt-get install swift-beta
      fi
script:
    - swift build
    - swift build -c release
    - swift test
